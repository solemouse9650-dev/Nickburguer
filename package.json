{
  "name": "burger-nick",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate:demo": "node src/seed/validateSeedCli.js",
    "check": "npm run validate:demo && npm run build",
    "verify:panel": "node --env-file=.env.local src/seed/verifyPanel.js",
    "seed": "node --env-file=.env.local src/seed/seed.js"
  },
  "dependencies": {
    "firebase": "^11.6.0"
  },
  "devDependencies": {
    "vite": "^6.2.5"
  }
}
